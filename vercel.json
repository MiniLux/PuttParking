{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter shared build && pnpm --filter client build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
